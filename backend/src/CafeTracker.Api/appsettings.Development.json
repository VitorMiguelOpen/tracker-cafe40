{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "//Database": "Em desenvolvimento usamos SQLite (arquivo local) para rodar sem Docker/Postgres. Para usar PostgreSQL local, troque Provider para 'Postgres' e ajuste a connection string.",
  "Database": {
    "Provider": "Sqlite"
  },
  "ConnectionStrings": {
    "Default": "Data Source=cafetracker.dev.db"
  }
}
